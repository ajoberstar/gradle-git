/**
 * The core structure of gradle-git's flavor of release behavior. The code
 * provided in this package can be used on its own, as long as users provide
 * their own implementation of {@code VersionStrategy}.
 */
package org.ajoberstar.gradle.git.release.base
